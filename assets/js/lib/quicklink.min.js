!((e, n) => {
	"object" === typeof exports && "undefined" !== typeof module
		? n(exports)
		: "function" === typeof define && define.amd
			? define(["exports"], n)
			: n((e.quicklink = {}));
})(this, (e) => {
	function n(e) {
		return new Promise((n, r, t) => {
			(t = new XMLHttpRequest()).open("GET", e, (t.withCredentials = !0)),
				(t.onload = () => {
					200 === t.status ? n() : r();
				}),
				t.send();
		});
	}
	let r,
		t =
			(r = document.createElement("link")).relList &&
			r.relList.supports?.("prefetch")
				? (e) =>
						new Promise((n, r, t) => {
							((t = document.createElement("link")).rel = "prefetch"),
								(t.href = e),
								(t.onload = n),
								(t.onerror = r),
								document.head.appendChild(t);
						})
				: n,
		o =
			window.requestIdleCallback ||
			((e) => {
				const n = Date.now();
				return setTimeout(() => {
					e({
						didTimeout: !1,
						timeRemaining: () => Math.max(0, 50 - (Date.now() - n)),
					});
				}, 1);
			}),
		i = new Set(),
		c = new Set(),
		u = !1;
	function a(e) {
		if (e) {
			if (e.saveData) return new Error("Save-Data is enabled");
			if (/2g/.test(e.effectiveType))
				return new Error("network conditions are poor");
		}
		return !0;
	}
	function s(e, r, _o) {
		const s = a(navigator.connection);
		return s instanceof Error
			? Promise.reject(new Error(`Cannot prefetch, ${s.message}`))
			: (c.size > 0 &&
					!u &&
					console.warn(
						"[Warning] You are using both prefetching and prerendering on the same document",
					),
				Promise.all(
					[].concat(e).map((e) => {
						if (!i.has(e))
							return (
								i.add(e),
								(r
									? (e) =>
											window.fetch ? fetch(e, { credentials: "include" }) : n(e)
									: t)(new URL(e, location.href).toString())
							);
					}),
				));
	}
	function f(e, _n) {
		const r = a(navigator.connection);
		if (r instanceof Error)
			return Promise.reject(new Error(`Cannot prerender, ${r.message}`));
		if (!HTMLScriptElement.supports("speculationrules"))
			return (
				s(e),
				Promise.reject(
					new Error(
						"This browser does not support the speculation rules API. Falling back to prefetch.",
					),
				)
			);
		if (document.querySelector('script[type="speculationrules"]'))
			return Promise.reject(
				new Error(
					"Speculation Rules is already defined and cannot be altered.",
				),
			);
		for (let t = 0, o = [].concat(e); t < o.length; t += 1) {
			const f = o[t];
			if (window.location.origin !== new URL(f, window.location.href).origin)
				return Promise.reject(
					new Error(`Only same origin URLs are allowed: ${f}`),
				);
			c.add(f);
		}
		i.size > 0 &&
			!u &&
			console.warn(
				"[Warning] You are using both prefetching and prerendering on the same document",
			);
		const l = ((e) => {
			const n = document.createElement("script");
			(n.type = "speculationrules"),
				(n.text =
					'{"prerender":[{"source": "list","urls": ["' +
					Array.from(e).join('","') +
					'"]}]}');
			try {
				document.head.appendChild(n);
			} catch (e) {
				return e;
			}
			return !0;
		})(c);
		return !0 === l ? Promise.resolve() : Promise.reject(l);
	}
	(e.listen = (e) => {
		if ((e || (e = {}), window.IntersectionObserver)) {
			const n = ((e) => {
					e = e || 1;
					let n = [],
						r = 0;
					function t() {
						r < e && n.length > 0 && (n.shift()(), r++);
					}
					return [
						(e) => {
							n.push(e) > 1 || t();
						},
						() => {
							r--, t();
						},
					];
				})(e.throttle || 1 / 0),
				r = n[0],
				t = n[1],
				a = e.limit || 1 / 0,
				l = e.origins || [location.hostname],
				d = e.ignores || [],
				h = e.delay || 0,
				p = [],
				m = e.timeoutFn || o,
				w = "function" === typeof e.hrefFn && e.hrefFn,
				g = e.prerender || !1;
			u = e.prerenderAndPrefetch || !1;
			var v = new IntersectionObserver(
				(n) => {
					n.forEach((n) => {
						if (n.isIntersecting)
							p.push((n = n.target).href),
								((e, n) => {
									n ? setTimeout(e, n) : e();
								})(() => {
									-1 !== p.indexOf(n.href) &&
										(v.unobserve(n),
										(u || g) && c.size < 1
											? f(w ? w(n) : n.href).catch((n) => {
													if (!e.onError) throw n;
													e.onError(n);
												})
											: i.size < a &&
												!g &&
												r(() => {
													s(w ? w(n) : n.href, e.priority)
														.then(t)
														.catch((n) => {
															t(), e.onError?.(n);
														});
												}));
								}, h);
						else {
							const o = p.indexOf((n = n.target).href);
							o > -1 && p.splice(o);
						}
					});
				},
				{ threshold: e.threshold || 0 },
			);
			return (
				m(
					() => {
						(e.el || document).querySelectorAll("a").forEach((e) => {
							(l.length && !l.includes(e.hostname)) ||
								(function e(n, r) {
									return Array.isArray(r)
										? r.some((r) => e(n, r))
										: (r.test || r).call(r, n.href, n);
								})(e, d) ||
								v.observe(e);
						});
					},
					{ timeout: e.timeout || 2e3 },
				),
				() => {
					i.clear(), v.disconnect();
				}
			);
		}
	}),
		(e.prefetch = s),
		(e.prerender = f);
});
