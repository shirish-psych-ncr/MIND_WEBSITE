"use strict";const ASCII_REGEX=/^[\u0000-\u007f]*$/u;let segmenter;function upperCaseFirst(e){return e.length<=1?e.toUpperCase():e[0].toUpperCase()+e.slice(1)}function getGraphemeCount(e){if(ASCII_REGEX.test(e))return e.length;segmenter??=new Intl.Segmenter("en-US");let t=0;for(const r of segmenter.segment(e))t++;return t}module.exports={upperCaseFirst:upperCaseFirst,getGraphemeCount:getGraphemeCount};