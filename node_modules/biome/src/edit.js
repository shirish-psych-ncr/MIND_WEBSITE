import editor from"editor";import path from"path";import{biomeFolderName}from"./constants";export default function edit(o){let t=path.join(biomeFolderName(),`${o}.json`);editor(t,o=>{console.log("Finished with code",o)})}