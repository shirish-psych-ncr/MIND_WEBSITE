import path from"path";import fs from"fs-promise";import{biomeFolderName}from"./constants";export default function preflight(){return fs.stat(biomeFolderName()).then(o=>!0).catch(o=>fs.mkdirp(biomeFolderName()))}