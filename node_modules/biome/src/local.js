import fs from"fs-promise";import Promise from"bluebird";import path from"path";import{biomeLocalName}from"./constants";export function getProjectMetadata(o,r=process.cwd()){let t=path.join(r,biomeLocalName());return fs.readJson(t)}