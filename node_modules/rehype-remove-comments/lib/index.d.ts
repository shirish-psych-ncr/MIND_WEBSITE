/**
 * Remove comments (except conditional comments).
 *
 * @param {Options | null | undefined} [options]
 *   Configuration (optional).
 * @returns
 *   Transform.
 */
export default function rehypeRemoveComments(options?: Options | null | undefined): (tree: Root) => undefined;
/**
 * Configuration.
 */
export type Options = {
    /**
     * Remove conditional comments (default: `false`);
     * the default is to leave them.
     */
    removeConditional?: boolean | null | undefined;
    /**
     * Choose which comments to keep.
     */
    test?: Test | null | undefined;
};
/**
 * Test a comment.
 */
export type Test = (value: string) => boolean | null | undefined;
import type { Root } from 'hast';
//# sourceMappingURL=index.d.ts.map