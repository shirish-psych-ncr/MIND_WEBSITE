/**
 * Check if a node is a conditional comment.
 *
 * @param {Nodes} node
 *   Node to check.
 * @returns {boolean}
 *   Whether `node` is a conditional comment.
 */
export function isConditionalComment(node: Nodes): boolean;
import type { Nodes } from 'hast';
//# sourceMappingURL=index.d.ts.map