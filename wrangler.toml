# Cloudflare Pages project configuration.
# Pages reads the output directory from this file when deploying with Wrangler.
# Redirects and response headers live in the root _redirects and _headers files.
name = "mind-grace-neuropsychiatric-clinic-website"
pages_build_output_dir = "."
