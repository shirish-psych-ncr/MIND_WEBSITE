Nothing should be ignored in this case, as the changes only include markdown documentation files (CHANGELOG.md, CONTRIBUTING.md, PROJECT_SUMMARY.md) which are source/config files and not build artifacts, dependencies, or temporary files.